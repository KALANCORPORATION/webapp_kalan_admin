export default function SearchBar() {
    return (
        <div className="search-bar">
            <input type="text" placeholder="Recherche" />
            {/* Ajouter une icône de loupe si nécessaire */}
        </div>
    );
}
